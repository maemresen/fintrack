spring:
    profiles:
        active: dev
    jpa:
        hibernate:
            ddl-auto: validate
        show-sql: false
    sql:
        init:
            mode: never
    liquibase:
        change-log: classpath:/db/changelog/db.changelog-master.yaml